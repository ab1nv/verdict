[tool.poetry]
name = "verdict"
version = "0.1.0"
description = "lawful second brain."
authors = ["Abhinav Singh"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
tk = "^0.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
